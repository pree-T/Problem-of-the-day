class Solution{
  public:
  long long int wineSelling(vector<int>& arr, int n){
      long long sum=0,ans=0;
      for(int i=0;i<n;i++)
      {
          sum+=arr[i];
          ans+=abs(sum);
      }
      return ans;
      
  }
};
